query ProductDetails($slug: String!, $channel: String!) {
	product(slug: $slug, channel: $channel) {
		id
		name
		slug
		description
		seoTitle
		seoDescription
		mdxFr: metafield(key: "mdxFr")
		mdxEn: metafield(key: "mdxEn")
		media {
			alt
			url(format: WEBP, size: 1024)
		}
		category {
			id
			name
		}
		attributes {
			attribute {
				id
				name
				slug
				translation(languageCode: EN) {
					name
				}
			}
			values {
				plainText
				inputType
			}
		}
		variants {
			...VariantDetails
		}
		pricing {
			priceRange {
				start {
					gross {
						amount
						currency
					}
				}
				stop {
					gross {
						amount
						currency
					}
				}
			}
		}
	}
}
