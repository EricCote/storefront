fragment ProductListItem on Product {
	id
	name
	slug
	fr: translation(languageCode: FR) {
		name
		slug
	}
	pricing {
		priceRange {
			start {
				gross {
					amount
					currency
				}
			}
			stop {
				gross {
					amount
					currency
				}
			}
		}
	}
	category {
		id
		name
		fr: translation(languageCode: FR) {
			name
		}
	}
	thumbnail(size: 1024, format: WEBP) {
		url
		alt
	}
}
